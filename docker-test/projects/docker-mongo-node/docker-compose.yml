db:
  image: mongo:3.0.4
  volumes:
    - db:/data/db
  ports:
    - "27017:27017"

web:
  build: .
  command: npm start
  ports:
    - "3000:3000"
  links:
    - db
  volumes:
    - ./webapp:/app

# docker stop $(docker ps -a -q)

# docker rm $(docker ps -a -q)

# docker rmi $(docker images -q)